services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: kobani
      POSTGRES_PASSWORD: kobani
      POSTGRES_DB: kobani
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
